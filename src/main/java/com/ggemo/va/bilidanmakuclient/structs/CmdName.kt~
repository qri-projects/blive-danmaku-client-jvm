package com.ggemo.va.bilidanmakuclient.structs

import java.util.*
import java.util.function.Function
import java.util.stream.Collectors

enum class CmdName(val rawValue: String) {
    HEART_BEAT(""), DANMAKU(""), SEND_GIFT(""), GUARD_BUY(""), SUPER_CHAT("");

    init {
        Arrays.stream(CmdName.values())
            .collect(Collectors.toMap({it.rawValue}, Function.identity()));
    }
    fun of(str: String) {

    }
}